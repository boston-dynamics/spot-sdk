-f ../../../prebuilt

bosdyn-client~=2.0
opencv-python~=4.4.0
Pillow==6.1.0
scipy~=1.4.1
# The following line is commented out because it can change depending on setup
# You may choose to install Tensorflow CPU if running from a non-CORE AI machine
# tensorflow-gpu~=2.0.4
